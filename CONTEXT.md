# Project Context

## 项目目标

使用 PySide6 构建连接 Binance 与 Bitget 正式生产环境的套利桌面应用。应用以现货腿和永续腿组成双腿订单，并在桌面 GUI 中呈现套利机会、订单和套利仓位。

## 当前状态

GUI 关联、非敏感配置、日志、内存凭据、REST/WebSocket 适配、交易标的与交易规则归一化、实时行情、深度和资金费率处理、套利机会计算、账户同步、双腿下单与成交对账，以及套利仓位和手动双腿平仓能力已实现。`ApplicationRuntime` 统一管理正式数据源、恢复上下文、后台线程和 Qt 生命周期。正式订单只允许由用户在 GUI 中明确准备并确认；没有自动开仓、自动平仓或业务风控。当前实现不假定任何尚未确定的交易策略、交易对、阈值或业务规则。

## 开发阶段

开发按以下顺序推进；后续阶段不得提前引入当前阶段之外的业务实现：

1. 项目基础与 GUI 关联。
2. 配置、日志与 API 凭据管理。
3. Binance、Bitget REST 基础适配。
4. Binance、Bitget WebSocket 基础适配。
5. 交易对匹配与交易规则归一化。
6. 行情、深度与资金费率处理。
7. 套利机会、手续费、滑点与利润计算。
8. 账户、余额、保证金与持仓同步。
9. 双腿下单、订单状态与成交对账。
10. 套利仓位管理、GUI 展示、手动双腿平仓与受控实盘联调。
11. 正式生产环境的人工端到端验收与交付验证。

本阶段划分只定义实施顺序，不定义具体策略、交易对、阈值或触发规则。

## 核心领域术语

- **交易腿**：一次套利组合中在单一交易所、市场和方向上执行的订单部分。
- **双腿订单**：为建立或处理同一套利组合而关联执行的两条交易腿。
- **套利机会**：由两条候选交易腿及其市场数据构成、等待既定业务规则评估的候选组合；当前不定义筛选阈值。
- **现货腿**：在 Binance 或 Bitget 现货市场执行的交易腿。
- **永续腿**：在 Binance 或 Bitget 永续合约市场执行的交易腿。
- **资金费率**：永续合约市场在资金费用结算中使用的费率，按交易所原始精度以 `Decimal` 表示。
- **价差**：两条相关市场价格之间的差值或相对差异，按明确口径以 `Decimal` 计算。
- **滑点**：预期成交价格与实际或估算成交价格之间的差异，以 `Decimal` 表示。
- **交易规则**：交易所对价格、数量和名义价值等订单参数施加的约束，包括 tick size、quantity step、minimum quantity、minimum notional 和 contract multiplier。
- **成交对账**：核对订单状态、各腿成交数量、成交价格和费用，使本地记录与交易所结果一致的过程。
- **套利仓位**：由相互关联的现货腿与永续腿及其已成交数量共同构成的仓位记录。

## 明确排除

- 模拟盘。
- 测试网。
- Demo Trading。
- 业务风控系统。
- 第三方交易所 SDK。

## 当前阶段范围

阶段 10 增加套利仓位、手动平仓和现有 GUI 的受控执行关联，包括：

- 只从阶段 9 已对账的两腿实际成交创建套利仓位。
- 记录每腿实际数量、均价、手续费、剩余数量、开仓时间和 `Decimal` 盈亏。
- 按现有仓位生成反向订单并复用集中量化、最小规则校验、客户端订单号幂等和超时查询恢复。
- 顺序推进平仓两腿，合并 REST 快照与私有 WebSocket 订单/成交事件，并明确报告部分成交、失败、未知状态和剩余数量。
- 失败后的再次手动平仓只使用当前剩余数量；若一条腿已清零，只提交另一条仍有余量的反向订单。
- 开仓状态未知时只查询原客户端订单号并继续对账，不盲目重复下单。
- 客户端订单号、未知订单、开仓/平仓请求及原仓位上下文持久化；重启恢复时先查询已提交的原订单，再恢复双腿状态机。
- 现有机会、余额、账户持仓、双腿下单和套利仓位控件通过 Presenter 与 Qt Model 接收统一领域数据。
- 阻塞式 REST 执行只在线程池后台运行，结果通过 Qt Signal 返回 GUI 主线程。
- 四个正式生产 REST 交易适配器在 Composition Root 创建；应用启动会连接正式行情和具备完整凭据的私有数据源，但不自动发送订单。

## 当前阶段不包括

- 自动开仓、自动平仓、止损、止盈或任何业务风控。
- 在自动化测试中执行真实账户请求、真实 WebSocket 连接或真实订单。
- 未经用户在 GUI 明确触发的正式订单。
- 数据库业务功能、策略阈值或自动机会选择规则。
